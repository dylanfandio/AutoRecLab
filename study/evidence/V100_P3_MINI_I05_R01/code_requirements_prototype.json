["Use exactly one local dataset file from ../../../study/data/ and inspect its actual schema before preprocessing.", "Use exactly one algorithm implementation from the installed recommender stack and record its fully-qualified class name and package version.", "Perform a per-user 80/20 train/test holdout with no validation split.", "Use a fixed random seed for both the split and model training.", "Evaluate only top-N ranking metrics needed for the prototype, with a single cutoff; ensure metrics are finite and computed from real recommendations.", "Produce at least one plot summarizing the prototype results.", "Persist the result table and reproducibility metadata to disk, including preprocessing counts and train/test interaction counts.", "Run a deterministic self-validation that verifies completeness, finiteness, and expected metric consistency for the chosen cutoff."]