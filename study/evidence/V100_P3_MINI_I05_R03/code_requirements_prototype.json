["Use exactly one dataset for the prototype stage; choose one local file already present under ../../../study/data/ and do not download data.", "Use exactly one algorithm for the prototype stage.", "Inspect the chosen raw file schema and map fields correctly before preprocessing.", "Perform only the dataset-specific implicit-feedback conversion needed for the chosen dataset.", "Apply only the minimal core filtering needed for the pilot if it is required to obtain valid train/test data.", "Use a per-user 80/20 train/test holdout with no validation split.", "Set and record a fixed random seed for both splitting and model training.", "Evaluate only minimal ranking metrics for the pilot: nDCG and Precision at a single cutoff k=10.", "Generate top-10 recommendations explicitly bounded to k=10.", "Verify that all computed metric values are finite and come from actual recommendations.", "Persist results incrementally so completed runs are retained if later steps fail.", "Produce at least one plot summarizing the pilot results.", "Record preprocessing counts and train/test interaction counts needed for reproducibility.", "Record the detected package versions and the exact algorithm class used."]