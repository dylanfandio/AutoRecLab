["Use LensKit 0.14.4 directly for algorithm training, recommendation generation, and evaluation; do not route the core experiment through OmniRec if that would change the requested backend.", "Use exactly three algorithms: ALS, ItemKNN, and Pop.", "Use exactly three datasets: MovieLens100K from u.data, Amazon Video Games from VideoGames.csv, and Last.FM from UserTaggedArtiststimestamps.dat in ../../../study/data/.", "Apply 5-core filtering to each dataset before splitting or training.", "For MovieLens100K and Amazon Video Games, convert ratings greater than 3 to implicit positive interactions before evaluation.", "Use five fixed random seeds for splitting and report the seed values.", "For each dataset and seed, perform a user-based 80/20 holdout split.", "Train all models with standard/default hyperparameters.", "Evaluate nDCG@1, nDCG@5, nDCG@10, Precision@1, Precision@5, and Precision@10.", "Compute metrics only on held-out test interactions and prevent leakage from test data into preprocessing or training.", "Aggregate metric results across the five seeds for each dataset-algorithm pair using means.", "Include a short statistical analysis of seed sensitivity using the across-seed spread/variance of the metrics.", "Preserve the existing script structure and outputs as much as possible; extend incrementally rather than rewriting from scratch.", "Produce tabular summaries and/or plots showing performance variation across seeds for each algorithm, dataset, and metric if the script already has output generation support."]