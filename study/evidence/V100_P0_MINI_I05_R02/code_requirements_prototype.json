["Use exactly one dataset: MovieLens100K.", "Use exactly one algorithm: ItemKNN only.", "Load the dataset with the project\u2019s dataset loader and apply only the preprocessing needed for the prototype: convert MovieLens ratings to implicit feedback, then apply 5-core filtering.", "Use one user-based 80/20 holdout split with a single random seed.", "Evaluate one ranking metric cutoff only: nDCG@10.", "Produce at least one plot summarizing the pilot result.", "Record the dataset, preprocessing steps, split ratio, seed, algorithm name, metric value, and plot path for reproducibility.", "Keep the workflow end-to-end in one prototype run: load, preprocess, split, train, evaluate, and plot."]