["Use exactly one dataset for the prototype: MovieLens100K loaded from the local file ../../../study/data/u.data.", "Use exactly one algorithm for the prototype: ItemKNN, instantiated through the installed OmniRec/LensKit integration and record its fully-qualified class name and resolved hyperparameters.", "Treat MovieLens ratings greater than 3 as implicit feedback, then apply 5-core filtering.", "Use a user-based 80/20 train/test holdout with no validation split.", "Seed both the split and model training for reproducibility.", "Evaluate only one cutoff for the pilot: k=10.", "Compute only the minimal ranking metrics needed for the pilot: nDCG@10 and Precision@10.", "Produce at least one plot from the results to validate the reporting pipeline.", "Persist results and intermediate artifacts so the pilot can be resumed or inspected after completion.", "Record preprocessing counts and train/test interaction counts for the chosen dataset.", "Verify that all metric outputs are finite values from actual recommendations and fail fast if expected result rows are missing or empty."]