["Use exactly one locally available dataset file from ../../../study/data/ and do not download replacement data.", "Use exactly one algorithm implementation and record its fully-qualified class name and package version.", "Preprocess the raw file according to its actual schema, then convert it to implicit top-N feedback if needed.", "Apply 5-core filtering before splitting.", "Use a user-based 80/20 train/test holdout with no validation split.", "Seed both data splitting and model training for reproducibility.", "Evaluate with a single cutoff and at least one ranking metric computed from real recommendations; results must be finite.", "Produce at least one plot from the experiment results.", "Persist the prototype configuration and results so the run can be reproduced."]