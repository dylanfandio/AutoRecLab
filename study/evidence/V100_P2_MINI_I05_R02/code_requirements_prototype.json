["Prototype stage: use exactly one dataset, one algorithm, one seed, and one cutoff to keep the pilot minimal and fast.", "Use OmniRec end-to-end for dataset loading, preprocessing, splitting, training, evaluation, checkpointing, and reporting; do not call backend libraries directly.", "Use the local MovieLens100K file `../../../study/data/u.data` only; do not download replacement data.", "Inspect the raw MovieLens100K schema before mapping it; parse it with the actual tab-delimited `user`, `item`, `rating`, `timestamp` columns.", "Convert MovieLens100K ratings to implicit interactions using the `rating > 3` rule.", "Apply 5-core filtering before splitting.", "Use a user-based 80/20 train/test holdout with no validation partition.", "Run exactly one algorithm class supported by OmniRec and record the exact class name used.", "Evaluate only nDCG@10 and Precision@10.", "Ensure all reported metrics are finite values computed from real recommendations; do not accept placeholders, synthetic values, NaNs, or empty-result rows as success.", "Persist results incrementally so completed runs are retained if a later run fails.", "Record the detected package versions and preprocessing/train-test interaction counts needed to reproduce the pilot experiment.", "Produce at least one plot from the resulting metrics to validate the reporting pipeline."]